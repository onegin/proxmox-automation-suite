# Debian Basic-Setting Script

## 🐧 Описание

Скрипт автоматической базовой настройки операционной системы Debian. Выполняет первоначальную конфигурацию системы, устанавливает необходимые пакеты и настраивает основные параметры.

## 📋 Функциональность

### Основные операции:
- ✅ Обновление списка пакетов и системы
- ✅ Установка базового набора утилит
- ✅ Создание пользователя с правами sudo
- ✅ Настройка локали и часового пояса
- ✅ Настройка SSH-доступа
- ✅ Изменение именования сетевых интерфейсов

### Устанавливаемые пакеты:
| Категория | Пакеты |
|-----------|--------|
| Редакторы | `vim` |
| Системные утилиты | `git`, `wget`, `curl`, `mc`, `unrar`, `zip`, `htop`, `iftop`, `screen` |
| Сетевое | `openssh-server`, `net-tools`, `zabbix-agent` |
| Администрирование | `sudo`, `locales`, `rsyslog`, `tzdata`, `logrotate` |

## 🚀 Быстрый старт

### Предварительные требования
- Debian 10/11/12/13
- Доступ с правами root
- Стабильное интернет-подключение

### Установка и запуск

```bash
# Скачать скрипт
wget https://raw.githubusercontent.com/username/repo/main/setup.sh

# Дать права на выполнение
chmod +x setup.sh

# Запуск
sudo ./setup.sh
```

### Процесс выполнения

1. **Автопроверка прав** - проверка прав администратора
2. **Обновление системы** - обновление пакетов и системы
3. **Установка пакетов** - установка всех необходимых утилит
4. **Настройка системы**:
   - Локализация (en_US.UTF-8)
   - Часовой пояс
   - SSH (разрешение root)
   - Именование интерфейсов (eth0, eth1)
5. **Создание пользователя** - запрос имени и пароля для нового пользователя

## ⚙️ Особенности настройки

### Сетевые интерфейсы
Скрипт изменяет именование сетевых интерфейсов на классический стиль (eth0, eth1 вместо предсказуемых имен)

### SSH конфигурация
Разрешает подключение пользователя root по SSH для удобства администрирования

### Безопасность
- Пароль пользователя вводится скрыто
- Пользователь добавляется в группу sudo
- Настраивается корректная локализация

## 🔄 После выполнения

**Требуется перезагрузка системы** для применения изменений в именовании сетевых интерфейсов:

```bash
sudo reboot
```

## 📝 Лицензия

✅ Бесплатно для некоммерческого использования

## 👨‍💻 Автор

**Антонов Евгений**  
📧 Контакты: ae@dcea.ru

## ⚠️ Важные примечания

- Скрипт предназначен для первоначальной настройки чистых систем
- Рекомендуется проверить на тестовой среде перед использованием в production
- После настройки рекомендуется дополнительно настроить firewall и безопасность SSH

## 🔧 Доработка

Для добавления дополнительного функционала или исправления ошибок создавайте issues и pull requests в репозитории проекта.

---
