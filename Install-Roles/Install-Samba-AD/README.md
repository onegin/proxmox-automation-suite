
# 🎯 Samba Active Directory Domain Controller Installer

Автоматический скрипт для установки Samba4 в качестве контроллера домена Active Directory на Debian.

## 📖 Описание

Этот скрипт автоматизирует процесс установки и настройки Samba4 в роли контроллера домена Active Directory. Он подходит для создания нового домена в тестовой или производственной среде.

## ✨ Возможности

- ✅ Автоматическая установка Samba4 и зависимостей
- 🏗️ Создание нового домена Active Directory
- 🌐 Настройка встроенного DNS-сервера
- 🔐 Интеграция с Kerberos для аутентификации
- 📊 Проверка работоспособности всех компонентов
- 🛡️ Резервное копирование существующих конфигураций

## 📋 Требования

- **ОС**: Debian 11/12 (Bullseye/Bookworm)
- **Права**: root-доступ
- **Память**: минимум 2 ГБ ОЗУ
- **Диск**: минимум 10 ГБ свободного места
- **Сеть**: статический IP-адрес

## 🚀 Быстрый старт

1. **Клонируйте или скачайте скрипт:**
   ```bash
   git clone https://github.com/onegin/proxmox-automation-suite.git
   ```

2. **Дайте права на выполнение:**
   ```bash
   chmod +x ad.sh
   ```

3. **Запустите скрипт:**
   ```bash
   sudo ./ad.sh
   ```

4. **Следуйте инструкциям:** Введите параметры домена когда скрипт запросит их.

## 📝 Параметры домена

При запуске скрипт запросит:
- **Имя домена** (FQDN): например, `company.local`
- **NetBIOS-имя**: например, `COMPANY` 
- **Пароль администратора**: минимум 8 символов

## 🛠️ Ручная установка (альтернатива)

Если скрипт не работает, выполните команды вручную:

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка пакетов
sudo apt install -y samba smbclient winbind krb5-user dnsutils

# Создание домена
sudo samba-tool domain provision --use-rfc2307 --realm=COMPANY.LOCAL --domain=COMPANY --server-role=dc --dns-backend=SAMBA_INTERNAL --adminpass=YourPassword123

# Запуск службы
sudo systemctl enable samba-ad-dc
sudo systemctl start samba-ad-dc
```

## 🔧 Проверка установки

После установки проверьте работоспособность:

```bash
# Проверка службы
systemctl status samba-ad-dc

# Проверка домена
samba-tool domain level show

# Проверка DNS
nslookup your-domain.local

# Проверка пользователей
samba-tool user list
```

## 📚 Полезные команды

### Управление пользователями
```bash
# Создать пользователя
samba-tool user create username

# Установить пароль пользователя
samba-tool user setpassword username

# Список пользователей
samba-tool user list

# Удалить пользователя
samba-tool user delete username
```

### Управление группами
```bash
# Создать группу
samba-tool group add groupname

# Добавить пользователя в группу
samba-tool group addmembers "Domain Admins" username

# Список групп
samba-tool group list
```

### Информация о домене
```bash
# Информация о домене
samba-tool domain info your-domain.local

# Уровень функциональности
samba-tool domain level show

# Время работы домена
samba-tool domain time show
```

## 🌐 Настройка клиентов

### Windows клиенты
1. Настройте DNS на IP-адрес контроллера домена
2. Присоедините компьютер к домену через "Система" → "Сведения о системе" → "Изменить параметры"

### Linux клиенты
```bash
# Установка необходимых пакетов
sudo apt install -y realmd sssd sssd-tools libnss-sss libpam-sss adcli

# Присоединение к домену
sudo realm discover your-domain.local
sudo realm join your-domain.local -U administrator
```

## ⚠️ Устранение неполадок

### Сброс конфигурации
Если нужно начать заново:
```bash
sudo systemctl stop samba-ad-dc
sudo rm -rf /var/lib/samba/*
sudo rm -f /etc/samba/smb.conf
```

### Проблемы с DNS
```bash
# Проверка DNS
nslookup your-domain.local

# Перезапуск службы
sudo systemctl restart samba-ad-dc
```

### Проблемы с временем
```bash
# Установка NTP
sudo apt install -y ntp
sudo systemctl enable ntp
sudo systemctl start ntp
```

## 🔒 Безопасность

- Настройте брандмауэр для открытия необходимых портов
- Регулярно обновляйте систему и Samba
- Используйте сложные пароли для учетных записей
- Настройте резервное копирование конфигурации

## 📄 Лицензия

Этот проект распространяется под лицензией MIT.

## 🤝 Вклад в развитие

Если вы нашли ошибку или хотите предложить улучшение:
1. Создайте Issue с описанием проблемы
2. Предложите Pull Request с исправлениями

## 📞 Поддержка

Если у вас возникли проблемы:
1. Проверьте логи: `journalctl -u samba-ad-dc`
2. Создайте Issue в репозитории проекта
3. Опишите вашу ОС, версию Debian и шаги для воспроизведения ошибки

---
**⭐ Если этот проект был полезен, поставьте звезду репозиторию!**
```

## 📁 Структура проекта

samba-ad-dc/
├── ad.sh              # Основной скрипт установки
├── README.md          # Документация
├── backup/            # Каталог для резервных копий (создается автоматически)
└── logs/              # Каталог для логов (опционально)

## 🎯 Особенности новой версии:

- **📋 Подробные комментарии** для каждого шага
- **🎨 Эмодзи** для лучшей визуализации процесса
- **🔧 Улучшенная обработка ошибок**
- **📚 Полная документация** в README.md
- **🛡️ Проверки безопасности**
- **📊 Проверка работоспособности** после установки
